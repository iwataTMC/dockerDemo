## 前提
以下の2つのコマンドが利用できること。
* docker
* docker-compose

## 使い方
* build

    ```bash
    docker-compose build
    ```

* start

    ```bash
    docker-compose up -d
    ```

* アクセス

    ```bash
    http://<マシンのIPアドレス>:8780
    ```